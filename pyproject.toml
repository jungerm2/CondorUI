[project]
name = "condor-webui"
version = "0.1.0"
description = "Web UI for submitting and monitoring HTCondor jobs"
requires-python = ">=3.14"
dependencies = [
    "flask>=3.1.3",
    "flask-sqlalchemy>=3.1.1",
    "htcondor>=25.10.1",
]

[project.scripts]
condor-webui = "run:main"
